{
  "image": "ubuntu:latest",
  "features": {
    "ghcr.io/devcontainers/features/rust:1": {}
  },
  "postCreateCommand": "apt update && apt install -y curl git build-essential pkg-config libssl-dev protobuf-compiler && curl https://cli.nexus.xyz/ | sh"
}
